// Environment variables to be maintained in process containers
env {
    KRAKEN2_DEFAULT_DB = "${params.kraken2_db}"
    KRAKEN2_DB_PATH    = "${params.kraken2_db}"
    BLASTDB            = "${params.blast_db}"
}
